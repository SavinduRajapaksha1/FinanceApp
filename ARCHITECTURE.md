# 🏗️ Architecture & Design Documentation

## System Architecture

### Clean Architecture Layers

```
┌─────────────────────────────────────────────────────────────┐
│                     PRESENTATION LAYER                       │
│  ┌────────────┐  ┌────────────┐  ┌────────────┐            │
│  │   Home     │  │Categories  │  │  History   │            │
│  │  Screen    │  │  Screen    │  │  Screen    │            │
│  └────────────┘  └────────────┘  └────────────┘            │
│         │                │                │                  │
│         └────────────────┴────────────────┘                  │
│                         │                                    │
│                  ┌──────▼──────┐                            │
│                  │   Widgets   │                            │
│                  │  (Atomic)   │                            │
│                  └─────────────┘                            │
├─────────────────────────────────────────────────────────────┤
│                     DOMAIN LAYER                             │
│  ┌────────────┐  ┌────────────┐  ┌────────────┐            │
│  │  Budget    │  │  Category  │  │ BudgetItem │            │
│  │  Entity    │  │  Entity    │  │   Entity   │            │
│  └────────────┘  └────────────┘  └────────────┘            │
│                         │                                    │
│  ┌────────────┐  ┌──────▼──────┐  ┌────────────┐            │
│  │Repository  │  │  Use Cases  │  │ Validators │            │
│  │Interfaces  │  │             │  │            │            │
│  └────────────┘  └─────────────┘  └────────────┘            │
├─────────────────────────────────────────────────────────────┤
│                      DATA LAYER                              │
│  ┌────────────┐  ┌────────────┐  ┌────────────┐            │
│  │ Firebase   │  │   Hive     │  │  Models    │            │
│  │ Firestore  │  │ (Local DB) │  │ (Freezed)  │            │
│  └────────────┘  └────────────┘  └────────────┘            │
│         │                │                │                  │
│         └────────────────┴────────────────┘                  │
│                         │                                    │
│                  ┌──────▼──────┐                            │
│                  │ Repositories│                            │
│                  │   (Impl)    │                            │
│                  └─────────────┘                            │
└─────────────────────────────────────────────────────────────┘
```

---

## Atomic Design System

### Component Hierarchy

```
                    ┌────────────┐
                    │   PAGES    │
                    │  (Screens) │
                    └─────┬──────┘
                          │
                    ┌─────▼──────┐
                    │ TEMPLATES  │
                    │  (Layouts) │
                    └─────┬──────┘
                          │
        ┌─────────────────┼─────────────────┐
        │                 │                 │
  ┌─────▼──────┐   ┌──────▼──────┐   ┌─────▼──────┐
  │ ORGANISMS  │   │ ORGANISMS   │   │ ORGANISMS  │
  │KPI Section │   │Quick Actions│   │Summary Tbl │
  └─────┬──────┘   └──────┬──────┘   └─────┬──────┘
        │                 │                 │
        └─────────────────┼─────────────────┘
                          │
        ┌─────────────────┼─────────────────┐
        │                 │                 │
  ┌─────▼──────┐   ┌──────▼──────┐   ┌─────▼──────┐
  │ MOLECULES  │   │ MOLECULES   │   │ MOLECULES  │
  │  KPI Card  │   │Month Select │   │Budget Row  │
  └─────┬──────┘   └──────┬──────┘   └─────┬──────┘
        │                 │                 │
        └─────────────────┼─────────────────┘
                          │
        ┌─────────────────┼─────────────────┐
        │                 │                 │
  ┌─────▼──────┐   ┌──────▼──────┐   ┌─────▼──────┐
  │   ATOMS    │   │   ATOMS     │   │   ATOMS    │
  │  Button    │   │ TextField   │   │  Loading   │
  └────────────┘   └─────────────┘   └────────────┘
```

---

## Data Flow

### Read Operation

```
┌─────────┐      ┌──────────┐      ┌──────────┐      ┌──────────┐
│  Widget │─────▶│ Provider │─────▶│Use Case  │─────▶│Repository│
│         │      │(Riverpod)│      │          │      │          │
└─────────┘      └──────────┘      └──────────┘      └────┬─────┘
                                                           │
                                                           ▼
                                                    ┌──────────┐
                                                    │Firestore │
                                                    │          │
                                                    └────┬─────┘
     ┌─────────────────────────────────────────────────┘
     │
     ▼
┌─────────┐      ┌──────────┐      ┌──────────┐      ┌──────────┐
│  Widget │◀─────│ Provider │◀─────│  Model   │◀─────│   Data   │
│(Updated)│      │(Notifies)│      │(Converted)│     │(Retrieved)│
└─────────┘      └──────────┘      └──────────┘      └──────────┘
```

### Write Operation

```
┌─────────┐      ┌──────────┐      ┌──────────┐
│  User   │─────▶│  Widget  │─────▶│ Provider │
│ Action  │      │(OnTap)   │      │          │
└─────────┘      └──────────┘      └────┬─────┘
                                        │
                                        ▼
                                 ┌──────────┐
                                 │Use Case  │
                                 │(Validate)│
                                 └────┬─────┘
                                      │
                                      ▼
                                 ┌──────────┐
                                 │Repository│
                                 │(Save)    │
                                 └────┬─────┘
                                      │
                                      ▼
                                 ┌──────────┐
                                 │Firestore │
                                 │(Persist) │
                                 └────┬─────┘
                                      │
     ┌────────────────────────────────┘
     │
     ▼
┌─────────┐      ┌──────────┐      ┌──────────┐
│  Widget │◀─────│ Provider │◀─────│  Success │
│(Refreshed)    │(Updates) │      │ Callback │
└─────────┘      └──────────┘      └──────────┘
```

---

## State Management (Riverpod)

### Provider Hierarchy

```
                    ┌────────────────┐
                    │  AuthProvider  │
                    │    (Global)    │
                    └───────┬────────┘
                            │
            ┌───────────────┼───────────────┐
            │               │               │
      ┌─────▼─────┐   ┌────▼─────┐   ┌─────▼─────┐
      │  Budget   │   │ Category │   │ Analytics │
      │ Provider  │   │ Provider │   │ Provider  │
      └─────┬─────┘   └────┬─────┘   └─────┬─────┘
            │              │               │
            └──────────────┼───────────────┘
                           │
                     ┌─────▼─────┐
                     │  Widgets  │
                     │(Consumers)│
                     └───────────┘
```

### State Updates

```
User Action → Widget Event → Provider Notified
                                    ↓
                              State Updated
                                    ↓
                            Widgets Rebuilt
                                    ↓
                              UI Updated
```

---

## Navigation Flow

```
                    ┌─────────────┐
                    │  MainScreen │
                    │(BottomNav)  │
                    └──────┬──────┘
                           │
         ┌─────────────────┼─────────────────┐
         │                 │                 │
    ┌────▼────┐      ┌─────▼──────┐    ┌────▼────┐
    │  Home   │      │Categories  │    │ History │
    │ Screen  │      │  Screen    │    │ Screen  │
    └────┬────┘      └─────┬──────┘    └────┬────┘
         │                 │                 │
         │           ┌─────▼──────┐          │
         │           │   Tabs     │          │
         │           │(5 types)   │          │
         │           └────────────┘          │
         │                                   │
         └───────────────┬───────────────────┘
                         │
                   ┌─────▼──────┐
                   │  Dialogs   │
                   │ (Add/Edit) │
                   └────────────┘
```

---

## File Organization

### By Feature (Vertical Slices)

```
presentation/screens/
├── home/
│   ├── home_screen.dart          # UI
│   ├── home_controller.dart      # Logic (future)
│   └── home_state.dart           # State (future)
├── categories/
│   ├── categories_screen.dart    # UI
│   ├── category_controller.dart  # Logic (future)
│   └── category_state.dart       # State (future)
└── history/
    ├── history_screen.dart       # UI
    ├── history_controller.dart   # Logic (future)
    └── history_state.dart        # State (future)
```

### By Type (Horizontal Layers)

```
presentation/widgets/
├── atoms/              # Reusable primitives
├── molecules/          # Combined components
└── organisms/          # Feature sections
```

---

## Design Patterns Used

### 1. Repository Pattern
```dart
abstract class BudgetRepository {
  Future<Budget> getBudget(String id);
  Future<void> saveBudget(Budget budget);
}

class BudgetRepositoryImpl implements BudgetRepository {
  // Implementation with Firebase
}
```

### 2. Provider Pattern (Riverpod)
```dart
final budgetProvider = FutureProvider<Budget>((ref) async {
  final repository = ref.watch(budgetRepositoryProvider);
  return repository.getCurrentBudget();
});
```

### 3. Singleton Pattern
```dart
class CurrencyFormatter {
  CurrencyFormatter._();
  static String format(double amount) { ... }
}
```

### 4. Factory Pattern
```dart
factory BudgetItem.fromJson(Map<String, dynamic> json) =>
    _$BudgetItemFromJson(json);
```

---

## Dependency Injection

### Using Riverpod

```
Provider Definition → Dependency Graph → Automatic Injection

Example:
┌────────────────────┐
│BudgetRepository    │  Created once
└────────┬───────────┘
         │ Injected into
         ▼
┌────────────────────┐
│BudgetProvider      │  Uses repository
└────────┬───────────┘
         │ Consumed by
         ▼
┌────────────────────┐
│Widget (Consumer)   │  Gets data
└────────────────────┘
```

---

## Performance Considerations

### Widget Rebuilding

```
Without Optimization:
Parent Widget Changes → All Children Rebuild ❌

With Optimization:
Parent Widget Changes → Only Affected Children Rebuild ✅
```

### Using Const Constructors

```dart
// ❌ Without const - Rebuilds every time
return AppButton(label: 'Save');

// ✅ With const - Never rebuilds if same
return const AppButton(label: 'Save');
```

### ListView Optimization

```dart
// ❌ Poor - Builds all items upfront
ListView(
  children: items.map((item) => ItemWidget(item)).toList(),
)

// ✅ Good - Lazy builds items on scroll
ListView.builder(
  itemCount: items.length,
  itemBuilder: (context, index) => ItemWidget(items[index]),
)
```

---

## Error Handling Strategy

```
         ┌─────────────┐
         │  Operation  │
         └──────┬──────┘
                │
         ┌──────▼──────┐
         │   Try/Catch │
         └──┬───────┬──┘
            │       │
      ┌─────▼──┐  ┌▼─────────┐
      │Success │  │  Error   │
      └────┬───┘  └──┬───────┘
           │         │
           │    ┌────▼──────┐
           │    │Log Error  │
           │    └────┬──────┘
           │         │
           │    ┌────▼──────┐
           │    │Show Toast │
           │    └────┬──────┘
           │         │
           └─────────┼────────┐
                     │        │
              ┌──────▼──┐  ┌──▼────────┐
              │Update UI│  │Retry Logic│
              └─────────┘  └───────────┘
```

---

## Security Considerations

### Firestore Rules (Future)

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Users can only access their own data
    match /users/{userId} {
      allow read, write: if request.auth.uid == userId;
      
      // Budgets belong to user
      match /budgets/{budgetId} {
        allow read, write: if request.auth.uid == userId;
      }
    }
  }
}
```

### Data Validation

```
Client Side:
Input → Validators → Formatted → Sent

Server Side (Firebase):
Received → Security Rules → Validated → Stored
```

---

## Testing Strategy

### Test Pyramid

```
              ┌──────────┐
              │   E2E    │  Few tests (Integration)
              │  Tests   │  Full user flows
              └────┬─────┘
           ┌───────▼────────┐
           │ Widget Tests   │  Some tests
           │ UI Components  │  Component behavior
           └────────┬───────┘
        ┌────────────▼───────────┐
        │    Unit Tests          │  Many tests
        │ Models, Utils, Logic   │  Pure functions
        └────────────────────────┘
```

### Coverage Goals

- Unit Tests: 80%+
- Widget Tests: 60%+
- Integration Tests: Critical paths

---

## Build & Deploy Pipeline (Future)

```
Code → Commit → CI/CD → Build → Test → Deploy

GitHub → GitHub Actions → Flutter Build → Run Tests → Deploy
   │                         │              │           │
   │                         ▼              ▼           ▼
   │                    iOS Archive    All Pass    App Store
   └────────────────────────────────────────────────────┘
```

---

## Scalability Plan

### Horizontal Scaling

```
Current:
Single App → Single Backend

Future:
Multiple Platforms → Shared Backend → Cloud Functions
    │                      │                 │
    ├─ iOS                 ├─ Firestore     ├─ Analytics
    ├─ Android             ├─ Storage       ├─ Notifications
    └─ Web                 └─ Auth          └─ Scheduled Jobs
```

### Code Reusability

```
Shared Core:
├── Business Logic (Domain Layer)
├── Data Models (Entities)
├── Utilities (Formatters, Validators)
└── Theme System

Platform Specific:
├── iOS UI Components
├── Android UI Components
└── Web UI Components
```

---

This architecture ensures:
✅ **Scalability** - Easy to add features
✅ **Maintainability** - Clear separation of concerns
✅ **Testability** - Isolated, pure functions
✅ **Performance** - Optimized rendering
✅ **Security** - Validated, protected data
